#include <stdio.h>

int main() {
    char str[1000];

    // Read input including spaces
    fgets(str, sizeof(str), stdin);

    // Print each character until newline or null terminator
    for (int i = 0; str[i] != '\0'; i++) {
        if (str[i] == '\n') {
            break; // Stop at newline added by fgets
        }
        printf("%c\n", str[i]);
    }

    return 0;
}
