#include <stdio.h>

int main() {
    int n, i, j, prime_flag;

    // Prompt the user for input
    printf("Enter a number (n): ");
    scanf("%d", &n);

    printf("Prime numbers from 1 to %d are: ", n);
    
    // Loop through all numbers from 2 to n
    for (i = 2; i <= n; i++) {
        // Assume the number is prime initially
        prime_flag = 1;

        // Check for factors from 2 up to i-1
        for (j = 2; j < i; j++) {
            if (i % j == 0) {
                // If a factor is found, it's not a prime number
                prime_flag = 0;
                break; // No need to check further factors
            }
        }

        // If prime_flag is still 1, the number is prime
        if (prime_flag == 1) {
            printf("%d ", i);
        }
    }

    printf("\n");

    return 0;
}
